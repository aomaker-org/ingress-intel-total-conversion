# CI/CD

This directory will hold Continuous Integration and Continuous Deployment pipeline configurations, for example, GitHub Actions workflows in a `.github/workflows` subdirectory (if this were in the root). For this template, we will keep it here.
